// Copy this file to `config.js` and fill the values.
// This file exposes `window.SUPABASE_URL` and `window.SUPABASE_ANON_KEY`

window.SUPABASE_URL = "https://your-project.supabase.co"; // replace
window.SUPABASE_ANON_KEY = "your-anon-key"; // replace

// Security note: Do NOT commit your real anon key to a public repo.
