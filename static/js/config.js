// Copy this file from `config.example.js` and fill the values for local development.
// Replace the placeholder values below with your Supabase project details.
// This file exposes `window.SUPABASE_URL` and `window.SUPABASE_ANON_KEY`.

window.SUPABASE_URL = "https://your-project.supabase.co"; // TODO: replace
window.SUPABASE_ANON_KEY = "your-anon-key"; // TODO: replace

// Security note: Do NOT commit your real anon key to a public repo.
